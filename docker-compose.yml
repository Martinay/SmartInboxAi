services:
  smartinbox:
    build: .
    container_name: smartinbox
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./inbox:/app/inbox
      - ./archive:/app/archive
      - ./pending:/app/pending
      - ./error:/app/error
